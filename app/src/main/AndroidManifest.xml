<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ghorabaa.cultureguide">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SignIn.MainActivity">
        </activity>

        <activity android:name=".SignUp.SignUpActivity">
        </activity>

        <activity
            android:name=".EventPage.CreateEventActivity"
            android:label="@string/title_activity_addevent"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>

        <activity
            android:name=".EventPage.EventMainActivity"
            android:label="@string/title_activity_event_main_activty"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity android:name=".EventPage.RemoveEventActivity">
            android:label="@string/title_activity_remove_event"
            android:theme="@style/AppTheme.NoActionBar"
        </activity>

        <activity
            android:name=".EventPage.UpdateEventActivity"
            android:label="@string/title_activity_update_event_activty"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:name=".HomePage"
            android:label="@string/title_activity_home_page"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

    </application>

</manifest>